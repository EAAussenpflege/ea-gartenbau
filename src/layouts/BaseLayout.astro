---
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;

const pathname = Astro.url.pathname.replace(/\/+$/, '') || '/';

const navLinks = [
  { href: '/', label: 'Startseite' },
  { href: '/dienstleistungen', label: 'Unsere Dienstleistungen' },
  { href: '/ueber-uns', label: 'Über uns' },
  { href: '/kontakt', label: 'Kontakt' },
];

const isActive = (href: string) => (href === '/' ? pathname === '/' : pathname.startsWith(href));
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- Markiert, dass JS läuft – nur dann werden Inhalte für die Scroll-Animation ausgeblendet. -->
    <script is:inline>
      document.documentElement.classList.add('js');
    </script>
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="apple-touch-icon" href="/favicon.png" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="theme-color" content="#5cb85c" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="/images/hero.jpg" />
    <meta property="og:locale" content="de_DE" />
  </head>

  <body>
    <div class="lesefortschritt" id="lesefortschritt" aria-hidden="true"></div>

    <header class="site-header" id="siteHeader">
      <div class="container site-header__inner">
        <a href="/" class="logo" aria-label="EA-Außenpflege – zur Startseite">
          <img
            class="logo__bild"
            src="/images/logo-mark.png"
            alt=""
            width="128"
            height="128"
            fetchpriority="high"
          />
          <span class="logo__text">
            EA-Außenpflege
            <small>Garten- &amp; Außenanlagen Service</small>
          </span>
        </a>

        <nav class="nav" id="mainNav" aria-label="Hauptnavigation">
          {
            navLinks.map((link) => (
              <a href={link.href} aria-current={isActive(link.href) ? 'page' : undefined}>
                {link.label}
              </a>
            ))
          }
          <a class="btn btn--primary btn--sm" href="/kontakt">Angebot anfordern</a>
        </nav>

        <div class="header-actions">
          <a class="btn btn--primary btn--sm" href="/kontakt">
            Angebot anfordern <span class="arrow" aria-hidden="true">→</span>
          </a>
          <button class="nav-toggle" id="navToggle" type="button" aria-expanded="false" aria-controls="mainNav" aria-label="Menü öffnen">
            <span></span><span></span><span></span>
          </button>
        </div>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer class="site-footer">
      <div class="container">
        <div class="footer-grid">
          <div class="footer-brand">
            <a href="/" class="logo logo--hell">
              <img
                class="logo__bild"
                src="/images/logo-mark-hell.png"
                alt=""
                width="128"
                height="128"
                loading="lazy"
              />
              <span class="logo__text">
                EA-Außenpflege
                <small>Garten- &amp; Außenanlagen Service</small>
              </span>
            </a>
            <p>
              Ihr Partner für Garten- und Landschaftsbau, Gartenpflege und die
              professionelle Reinigung von Fassaden.
            </p>
          </div>

          <div>
            <h4>Leistungen</h4>
            <ul class="footer-list">
              <li><a href="/dienstleistungen">Garten- &amp; Landschaftsbau</a></li>
              <li><a href="/dienstleistungen">Gartenpflege</a></li>
              <li><a href="/dienstleistungen">Fassadenreinigung</a></li>
            </ul>
          </div>

          <div>
            <h4>Unternehmen</h4>
            <ul class="footer-list">
              <li><a href="/ueber-uns">Über uns</a></li>
              <li><a href="/dienstleistungen">Unsere Dienstleistungen</a></li>
              <li><a href="/kontakt">Kontakt</a></li>
            </ul>
          </div>

          <div>
            <h4>Kontakt</h4>
            <ul class="footer-list">
              <li><a href="tel:+4917621190179">0176 21190179</a></li>
              <li><a href="mailto:kontakt@ea-aussenpflege.de">kontakt@ea-aussenpflege.de</a></li>
              <li>Weinheim und Umgebung</li>
              <li>Mo–Fr 07:00–17:00 Uhr</li>
            </ul>
          </div>
        </div>

        <div class="footer-bottom">
          <span>© {new Date().getFullYear()} EA-Außenpflege. Alle Rechte vorbehalten.</span>
          <div class="footer-legal">
            <a href="/impressum">Impressum</a>
            <a href="/datenschutz">Datenschutz</a>
          </div>
        </div>
      </div>
    </footer>

    <script>
      // --- Header: Schatten beim Scrollen ---
      const header = document.getElementById('siteHeader');
      const onScroll = () => header?.classList.toggle('is-scrolled', window.scrollY > 8);
      onScroll();
      window.addEventListener('scroll', onScroll, { passive: true });

      // --- Lesefortschritt und Parallax im Titelbild ---
      // Beides hängt am selben Scroll-Ereignis und wird über
      // requestAnimationFrame gebündelt, damit das Scrollen flüssig bleibt.
      const fortschritt = document.getElementById('lesefortschritt');
      const titelbild = document.querySelector('.hero__bg');
      const wenigerBewegung = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      if (fortschritt || (titelbild && !wenigerBewegung)) {
        let angefordert = false;

        const zeichnen = () => {
          angefordert = false;
          const position = window.scrollY;

          if (fortschritt) {
            const strecke = document.documentElement.scrollHeight - window.innerHeight;
            const anteil = strecke > 0 ? Math.min(position / strecke, 1) : 0;
            fortschritt.style.transform = `scaleX(${anteil})`;
          }

          // Das Titelbild wandert langsamer als die Seite – aber nur,
          // solange es überhaupt sichtbar ist.
          if (titelbild instanceof HTMLElement && !wenigerBewegung) {
            if (position < window.innerHeight * 1.2) {
              titelbild.style.transform = `translate3d(0, ${position * 0.22}px, 0)`;
            }
          }
        };

        const anfordern = () => {
          if (angefordert) return;
          angefordert = true;
          window.requestAnimationFrame(zeichnen);
        };

        zeichnen();
        window.addEventListener('scroll', anfordern, { passive: true });
        window.addEventListener('resize', anfordern, { passive: true });
      }

      // --- Mobile Navigation ---
      const toggle = document.getElementById('navToggle');
      toggle?.addEventListener('click', () => {
        const open = header?.classList.toggle('is-open') ?? false;
        toggle.setAttribute('aria-expanded', String(open));
        toggle.setAttribute('aria-label', open ? 'Menü schließen' : 'Menü öffnen');
      });

      document.getElementById('mainNav')?.addEventListener('click', (event) => {
        if (event.target instanceof Element && event.target.closest('a')) {
          header?.classList.remove('is-open');
          toggle?.setAttribute('aria-expanded', 'false');
        }
      });

      // --- Scroll-Reveal ---
      const revealItems = document.querySelectorAll('[data-reveal]');
      const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      if (reduced || !('IntersectionObserver' in window)) {
        revealItems.forEach((el) => el.classList.add('is-visible'));
      } else {
        let observerFired = false;

        const observer = new IntersectionObserver(
          (entries) => {
            observerFired = true;
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add('is-visible');
                observer.unobserve(entry.target);
              }
            });
          },
          { rootMargin: '0px 0px -12% 0px', threshold: 0.12 },
        );

        revealItems.forEach((el) => observer.observe(el));

        // Sicherheitsnetz: sollte der Observer nie auslösen, wird der Inhalt trotzdem sichtbar.
        window.addEventListener('load', () => {
          window.setTimeout(() => {
            if (!observerFired) revealItems.forEach((el) => el.classList.add('is-visible'));
          }, 1200);
        });
      }
    </script>
  </body>
</html>
